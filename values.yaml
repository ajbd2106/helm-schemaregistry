# Default values for helm-schema-registry.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
Image: "armdocker.rnd.ericsson.se/aia/base/aia-docker-schemaregistry:latest"
Port: "8081"
